# Historical React/Vinext prototype

This is an archival snapshot, excluded from the static build and deployment.
Its package.json records the old dependencies, not dependencies of the live site.
Paths in this snapshot retain their original context and are not an active development entry point.
Use the repository-root `npm run dev` and `npm run build` for the maintained site.
