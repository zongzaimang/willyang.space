# 架构迁移记录：前四阶段

- 远端仓库：`zongzaimang/willyang.space`，正式分支 `main`。
- 迁移基线：`d96899435683f1e940f3fdf06ed44a2bb1ea9fa7`。
- 已查证该版本的部署运行 `35281693346` 成功；本次改造尚未据此宣称上线。
- 本地原先没有提交历史。通过 GitHub 读取并逐个哈希验证远端对象，接入同一提交为浅历史基线，建立 `codex/content-publishing` 工作分支，没有覆盖工作文件。
- 原始文件和素材哈希快照保存在忽略目录 `outputs/architecture-baseline/`；不要作为网站文件上传。
- 原有 5 个项目和 11 个生成页面地址保持兼容；样式和交互脚本未改造。
- 原有项目文案原样迁移，真实职责与更具体的图片说明留待内容编辑确认。
- React/Vinext 原型归档到 `legacy/prototype/`；旧生成页面归档到 `legacy/static-snapshot/`。只维护 content、tools 和正式 CSS/JS。
- 已存在的素材及文件结尾差异不属于本次功能改造，不应为提交方便而批量重置或混入提交。

实现范围：结构化内容与状态、图片导入差异和不可变快照、正式产物白名单、失败保护、独立预览、流程和浏览器测试、CI 检查及部署、发布包留存、明确选择历史运行回滚、部署后逐文件验证。

不包含：视觉重设计、CMS 登录后台、搜索筛选、多语言、项目文案重写或搜索优化。

## 本地验证结果

- 8 项发布流程测试全部通过。
- 54 组页面/宽度/主题检查通过，另验证旧地址、图片查看、焦点返回、无 JavaScript 阅读、404 和源文件隔离。
- 图片流程验证通过：尺寸、透明通道、原图不覆盖、缓存复用、源图更新及旧变体排除。
- 对照迁移前快照，11 份生成 HTML 内容一致（仅统一换行后比较），175 个原素材文件哈希均未改变。
- 发布素材由整个 assets 目录的 16,928,364 字节收敛到实际引用的 9,131,575 字节。
- 本地发布版本 `ffb8661293266bbda355`，174 个文件；发布后验证器在本地 HTTP 服务上核验其中 172 个公开内容文件与 404（CNAME、.nojekyll 不作为公开内容请求）。
- 以上为发布前的本地验证记录；实际提交、CI 运行和线上验证结果以 GitHub Actions 的对应发布记录为准。
